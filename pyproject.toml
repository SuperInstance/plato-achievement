[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-achievement"
version = "0.1.0"
description = "Achievement tracking — loss metric, milestones, progress"
license = {text = "MIT"}
requires-python = ">=3.8"
